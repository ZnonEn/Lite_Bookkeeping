# 个人记账 App 开发文档

## 1. 项目概述

本项目旨在开发一款**仅限个人使用**的 Android 记账应用，核心目标是尽可能自动化地记录日常收支，减少手动输入。主要依赖安卓无障碍服务实现自动记账，支持从微信、支付宝导入账单，并提供简单的搜索与自动分类功能。项目不包含多用户、云同步、社交等冗余功能，强调轻量、本地、隐私。

## 2. 功能需求

### 2.1 自动记账（基于无障碍服务）

- 通过 Android AccessibilityService 监听目标应用（微信、支付宝、银行 App 等）的通知栏消息和界面变化。
- 识别包含金额、交易类型（收入/支出）、交易对手等关键信息的文本。
- 自动提取交易数据并写入本地数据库。
- 支持用户设置监听范围（仅微信/支付宝/全部），避免干扰。
- 识别失败时不做记录，但可提示用户手动补录。

### 2.2 自动分类

- 根据交易描述、商户名称、备注等信息自动归类到预设支出类别（餐饮、交通、购物、娱乐、居住、医疗、其他等）。
- 采用 **本地规则匹配 + 简单机器学习（可选）**：
  - 初期使用关键词/正则规则（如“美团”“饿了么” → 餐饮）。
  - 允许用户手动修改分类，系统记录修正并逐步优化规则（本地保存自定义映射）。
- 支持收入类别（工资、红包、理财等）的分类。

### 2.3 搜索账目

- 提供简单搜索框，支持按金额、备注、分类、日期范围等条件模糊搜索。
- 搜索结果以列表展示，点击可查看详情或编辑。
- 搜索仅本地数据库查询，无需网络。

### 2.4 自动导入微信与支付宝账单

两种导入方式，用户可任选：

1. **文件导入**（推荐，稳定）：
   - 用户从微信/支付宝官方渠道导出账单文件（CSV 或 Excel）。
   - 应用读取文件，解析数据（时间、金额、类型、商户等）。
   - 自动去重（根据时间+金额+商户生成哈希），避免重复导入。
   - 解析后统一进入本地数据库，并触发自动分类。

2. **无障碍抓取**（辅助，实时）：
   - 利用无障碍服务监听微信/支付宝的“账单详情”页面，自动抓取单笔交易。
   - 适合零散补录，不适合大批量历史数据。
   - 需用户授权并保持应用在前台或后台运行。

## 3. 非功能需求

- **运行环境**：Android 8.0 及以上，单用户，无账号体系。
- **存储**：使用 Room 数据库本地存储，不依赖网络，数据不出设备。
- **性能**：无障碍服务需低内存占用，避免影响系统流畅性。
- **隐私**：不收集任何数据，不请求网络权限（除非未来增加备份功能）。
- **界面**：极简，单 Activity 多 Fragment，使用 Jetpack Compose 或传统 View 实现。
- **可维护性**：代码模块化，分类规则可配置。

## 4. 技术栈建议

| 模块         | 技术选型                              |
|--------------|---------------------------------------|
| 开发语言     | Kotlin                                |
| UI           | Jetpack Compose（或 XML + Material3）|
| 数据库       | Room                                  |
| 无障碍服务   | AccessibilityService                  |
| 文件解析     | Apache Commons CSV / POI（精简版）    |
| 后台任务     | WorkManager（定期清理、备份等）       |
| 依赖注入     | Hilt（可选，项目小可不引入）          |
| 本地规则引擎 | 自研简单规则匹配，无需重型框架       |

## 5. 数据模型

核心表：`transactions`

| 字段          | 类型      | 说明                               |
|---------------|-----------|------------------------------------|
| id            | Long (PK) | 自增主键                           |
| amount        | Double    | 金额，正数为收入，负数为支出       |
| category      | String    | 分类名称（如“餐饮”）               |
| note          | String?   | 备注/交易描述                      |
| merchant      | String?   | 商户名称（如“美团”）               |
| timestamp     | Long      | 交易时间（毫秒时间戳）             |
| source        | String    | 来源：manual / wechat / alipay / auto |
| rawData       | String?   | 原始数据（JSON，便于回溯）         |
| hash          | String    | 去重哈希（时间+金额+商户+来源）    |
| createdAt     | Long      | 记录创建时间                       |
| updatedAt     | Long      | 最后更新时间                       |

索引：`timestamp`、`category`、`hash`（唯一）。

## 6. 关键流程

### 6.1 自动记账流程（无障碍）

1. 用户首次开启无障碍服务并授权。
2. 服务监听目标应用的通知或窗口内容变化。
3. 当检测到包含金额和交易特征（如“支付成功”“收款”）的文本时，触发解析。
4. 提取金额、类型、商户/备注等信息。
5. 生成本地哈希，查询数据库是否已存在，去重。
6. 若为新交易，执行自动分类，写入数据库。
7. 可选：发送通知提醒用户“已自动记录一笔支出”。

### 6.2 账单导入流程（文件）

1. 用户点击“导入账单”，选择微信或支付宝。
2. 引导用户从对应 App 导出账单文件（提供简要步骤说明）。
3. 用户选择文件（通过系统文件选择器）。
4. 应用读取并解析 CSV/XLS 文件，提取交易记录。
5. 逐条生成本地哈希并检查去重。
6. 对新记录进行自动分类后写入数据库。
7. 显示导入结果（成功 N 条，重复 M 条，失败 K 条）。

### 6.3 搜索流程

1. 用户在主界面点击搜索图标。
2. 输入关键词或选择筛选条件（日期范围、分类等）。
3. 应用执行 Room 查询，按时间倒序返回结果。
4. 列表展示，支持点击查看详情、编辑或删除。

## 7. 自动分类规则（初始版）

规则采用**优先级匹配**，命中即返回分类。

### 示例规则（可配置）

| 关键词/正则                          | 分类   |
|--------------------------------------|--------|
| 美团、饿了么、外卖、餐饮、饭店、咖啡 | 餐饮   |
| 滴滴、地铁、公交、加油、停车         | 交通   |
| 淘宝、京东、拼多多、超市、便利店     | 购物   |
| 电影、KTV、游戏、旅游、健身          | 娱乐   |
| 房租、水费、电费、燃气、物业         | 居住   |
| 医院、药房、诊所、保险               | 医疗   |
| 转账、红包、退款、工资、奖金         | 收入相关 |

- 规则保存在本地 `category_rules` 表中，用户可添加/删除自定义规则。
- 当用户手动修改某笔交易分类时，将商户/描述关键词加入自定义规则（自动学习）。

## 8. 界面设计（简略）

- **主页面**：本月收支总览（收入、支出、结余），最近交易列表。
- **记一笔**：手动记账入口（可选，保留用于补录）。
- **搜索页**：顶部搜索框，下方筛选条（日期、分类）。
- **设置页**：
  - 无障碍服务开关
  - 账单导入
  - 分类规则管理
  - 数据导出/备份（可选）

## 9. 开发注意事项

- **无障碍服务稳定性**：需处理不同安卓版本、不同微信/支付宝版本导致的文本格式差异，解析逻辑应健壮。
- **权限申请**：仅需无障碍权限和存储读取权限（文件导入时）。不申请网络、定位等无关权限。
- **去重哈希**：建议使用 `MD5(timestamp + amount + merchant + source)`，确保唯一性。
- **数据备份**：个人使用，可提供 JSON 导出，方便迁移。

## 10. 里程碑规划（可选）

1. MVP：手动记账 + 本地数据库 + 简单分类。
2. 集成无障碍自动记账。
3. 支持微信/支付宝账单文件导入。
4. 完善搜索与分类优化。
5. 界面美化与稳定性测试。

---

*本文档旨在指导个人开发者快速实现核心功能，避免过度设计。*